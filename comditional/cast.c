#include <stdio.h>

int main(void)
{
	/*int a;
	a = sizeof(int) + sizeof(char); //int 4byte, char 1byte
	printf("%d", a);*/

	/*int a = 7 + 6;
	int b = (int)7.3 + (int)6.7;	//소수점을 자르고 계산
	printf("%d %d", a, b);*/

	/*float t;
	t = 10 / 3;	//정수끼리의 연산이라 변수를 실수로 설정하여도 정수로 나옴
	printf("%f\n", t);
	t = (float)10 / 3;	//숫자 앞에 float를 붙임으로 실수로 만들어 실수값이 나오게 한다
	printf("%f", t);*/

	return 0;
}
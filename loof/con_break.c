//1~10까지 숫자 출력
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	//for (int i = 1; i <= 10;i++)
	//{
	//	//7의 배수를 만나면 탈출
	//	if (i % 7 == 0)
	//		//break;
	//		continue;
	//	//continue:아래문장으로 내려가지않고
	//	//다시 반복문의 조건으로 올라가서 물어보고 반복
	//	printf("%d\n", i);
	//	
	//}

    //int sum = 0;

    //for (int i = 1; i <= 3; i++) {
    //    if (i == 2)
    //        continue;
    //    sum += i;
    //}
    //printf("continue 결과는 %d\n", sum);
    //printf("\n\n");

    //int num = 1;
    //for (int i = 1; ; i++) { //조건식이 없음
    //    num = num * i;
    //    if (i > 3)
    //        break;
    //}
    //printf("break문제 결과는 %d\n", num);

    //int i, cnt = 0;
    //for (i = 1;i <= 100;i++)
    //{
    //    if (i % 3 == 0 && i % 7 == 0)   //i를 3이랑 7로 나눴을때 나머지가 0인 수이면 출력
    //    {
    //        printf("%d*", i);   //3괄 7로 나눴을때 나머지가 0인 수들 출력(1~100)
    //    }
    //}

    //int i;
    //for (i = 0;i < 10;i++)
    //{
    //    if (i % 3 == 0)
    //        continue;
    //    //i를 3으로 나눴을때 0이면 넘어감
    //    if (i > 5)
    //        break;
    //    //i가 5가 되면 탈출
    //    printf("%d",i); //1245가 출력됨, 3은 넘어가졌기에 출력 안됨
    //}

    int 

	return 0;
}